# Production frontend profile.
# Copy to `.env.production` and adjust for your deployment.

# Use "/api" when the production frontend proxies API requests to the backend.
# Use "https://api.your-domain.example/api" when the API is on a separate host.
VITE_API_URL="/api"
VITE_SENTRY_DSN=
